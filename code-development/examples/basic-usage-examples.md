## Examples

```bash
# Feature implementation with email parser
/code-development --task="Add new RealtyMX email parser" --scope="small-feature"

# Bug fix with issue reference
/code-development --task="Fix authentication bug in PROJ-12345" --scope="bug-fix"

# Large refactoring with specific files
/code-development --task="Refactor contact import logic" --scope="large-refactoring" --files="apps/richdesk/controllers/ContactsController.php"

# Critical security fix
/code-development --task="Fix SQL injection in user search" --scope="critical-path"
```

